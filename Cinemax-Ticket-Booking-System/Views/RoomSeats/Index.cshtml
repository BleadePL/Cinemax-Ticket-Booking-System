@model IEnumerable<Cinemax_Ticket_Booking_System.Models.RoomSeat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Row)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Column)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScreeningRoom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Row)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Column)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScreeningRoom.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRS">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRS">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRS">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
